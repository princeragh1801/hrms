export enum Role {
    Employee = 0,
    Admin = 1,
    SuperAdmin = 2,
}

export enum Status {
    Pending = 0,
    Active = 1,
    Completed = 2,
}

export enum TaskType {
    Epic = 0,
    Feature = 1,
    Userstory = 2,
    Task = 3,
    Bug = 4,
}
export enum SortedOrder {
    Descending = 0,
    Ascending = 1,
    NoOrder = 2,
}